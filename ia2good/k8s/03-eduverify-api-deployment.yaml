apiVersion: apps/v1
kind: Deployment
metadata:
  name: eduverify-api
  namespace: ia2good
  labels:
    app: eduverify-api
    module: eduverify
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eduverify-api
  template:
    metadata:
      labels:
        app: eduverify-api
        module: eduverify
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      containers:
      - name: eduverify-api
        image: eduverify-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8002
          name: http
        - containerPort: 9090
          name: metrics
        
        envFrom:
        - configMapRef:
            name: ia2good-config
        - secretRef:
            name: ia2good-secrets
        
        env:
        - name: MODULE_NAME
          value: "eduverify"
        - name: PORT
          value: "8002"
        
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 2000m
            memory: 4Gi
        
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        
        readinessProbe:
          httpGet:
            path: /ready
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: eduverify-api-service
  namespace: ia2good
spec:
  selector:
    app: eduverify-api
  ports:
  - name: http
    port: 8002
    targetPort: 8002
  - name: metrics
    port: 9090
    targetPort: 9090
  type: ClusterIP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: eduverify-api-hpa
  namespace: ia2good
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eduverify-api
  minReplicas: 2
  maxReplicas: 8
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
