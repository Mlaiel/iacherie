apiVersion: v1
kind: Service
metadata:
  name: ainflue-api
  namespace: ainflue-staging
  labels:
    app: ainflue
    environment: staging
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: ainflue
  sessionAffinity: None