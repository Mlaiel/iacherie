# Database Production Requirements
# Additional dependencies for production database deployment

# Migration and schema management
alembic>=1.12.0

# SSL/TLS certificate management
cryptography>=41.0.7

# System monitoring
psutil>=5.9.0

# Performance testing and benchmarking
pytest-benchmark>=4.0.0

# PostgreSQL extensions (if not already installed)
pg-stat-kcache  # Query performance monitoring extension

# Connection pooling enhancements
pgbouncer  # External connection pooler (install separately)

# Backup and recovery tools
pg_dump  # PostgreSQL backup utility (included with PostgreSQL)
pg_restore  # PostgreSQL restore utility (included with PostgreSQL)

# WAL-E for WAL archiving (optional)
# wal-e>=1.1.1

# Additional security
python-gnupg>=0.5.0  # For backup encryption

# Monitoring and metrics
prometheus-client>=0.19.0